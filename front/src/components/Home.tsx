import { Typography } from "@mui/material";

const Home: React.FC = () => {
  return (
    <Typography variant="h4" sx={{ textAlign: "center", fontWeight: "bold" }}>
      Find and share rides with fellow Drexel students!
    </Typography>
  );
};

export default Home;